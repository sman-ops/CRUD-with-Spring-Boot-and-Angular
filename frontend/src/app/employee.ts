export interface Employee {
  id?: number;
  name: string;
  email: string;
  jobTitle: string;
  phone: string;
  imageUrl: string;
  employeeCode?: string;
}
// this represent  the type of data  that's going to be returning  whenever  we mak those calls from the backend
// the employee looks like from the backend
